# Testing

Every feature change must add or update compatibility tests for every supported operating system: macOS, Linux, Windows, and OpenWrt. The full compatibility matrix must pass before the change is complete.
